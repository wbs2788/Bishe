\chapter{文生视频安全防御原型系统的设计与实现}

\section{引言}

在前序章节中，本研究分别从输入端的指令审计与模型端的内生安全两个方面，提出了基于思维链增强的轻量化检测架构以及基于潜空间偏好优化的概念擦除方法。尽管这些独立的算法模块在离线实验环境中均展现出了优异的防御性能，但文生视频任务在实际工业部署中面临着更为复杂的工程挑战，包括高并发下的推理延迟、异构模型间的显存调度以及用户交互的流畅度等。单一的算法验证尚不足以证明“全流程防御体系”在真实业务场景中的鲁棒性与可行性。因此，构建原型系统是验证本研究防御算法在实际工程部署中可行性与有效性的关键步骤。

本系统旨在验证该文生视频防御方案在实际部署中的高效性与安全性。本系统的设计需平衡三个核心指标：首先，安全性是系统的核心基石，需确保对高风险内容的有效拦截；其次，实时性是工程化的关键，前置防御模块必须在毫秒级内完成响应；最后，易用性要求系统提供直观的交互及审计反馈。本章将围绕上述目标，详细阐述文生视频安全防御原型系统的总体架构设计、关键模块的工程实现细节以及系统级的性能测试结果。

本章余下内容的安排如下：
\begin{itemize}
    \item 第 5.2 节将通过系统需求分析确立全流程防御的业务逻辑，并展示包含表现层、应用逻辑层及核心模块层在内的五层分层软件架构设计。
    \item 第 5.3 节将深入探讨关键功能模块的集成细节，重点分析前置指令拦截与后置特征擦除机制的协同防御效果。
    \item 第 5.4 节将针对消费级硬件的资源约束，介绍权重量化、动态显存卸载以及 Flash Attention 2 等系统部署与推理加速优化策略。
    \item 第 5.5 节将通过典型防御案例（如医疗教育场景与隐晦意图攻击）及系统负载压测，从功能效能与工程鲁棒性两个方面对原型系统进行全面评估。
    \item 第 5.6 节将对全章内容进行总结，归纳本系统在打通算法研究与工业应用中的关键作用。
\end{itemize}

\section{系统需求分析与总体架构}

为了将前文提出的输入端检测算法与模型端概念擦除算法转化为可实际部署的安全防御能力，本节首先从业务流程的角度梳理了数据在系统内部的流转逻辑，随后自顶向下设计了高内聚低耦合的分层软件架构。

\subsection{全流程防御业务逻辑}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{Img/safety_defense.png}
    \bicaption{文生视频安全防御全流程业务逻辑图}{Safety defense workflow for video generation}
    \label{fig:safety_defense}
\end{figure}

相较于传统视频生成服务，本系统在架构中引入了指令安全审计流程。如图~\ref{fig:safety_defense}所示，该业务流程在逻辑上被划分为指令审计、安全生成以及结果反馈与审计三个紧密耦合的关键阶段，各阶段协同工作以确保内容的端到端安全。

业务流程始于用户在前端交互界面提交的自然语言提示词。这些原始指令并不会直接触发生成任务，而是首先被路由至基于 Llama-3.1 构建的前置检测模块。该模块作为前置检测环节，通过思维链推理机制分析输入文本的语义意图。系统依据推理结果对指令的风险等级进行实时判定。一旦识别出包含暴力、色情或非法行为等高风险要素的违禁内容，防御机制将立即被激活。系统会强制终止后续的生成流程，并向用户返回明确的拒绝理由及具体的违规类别提示。只有当指令通过了严格的安全性校验并被判定为无害后，数据流才会被允许放行至下一处理环节。

顺利通过审计的指令随即进入核心的视频生成阶段。在此阶段，系统调用经过潜空间偏好优化微调的 Wan-2.1 视频生成模型执行推理任务。不同于常规的生成过程，该模型集成了内生安全机制。在将文本特征映射为视频潜变量的过程中，经过微调的参数会自动抑制潜在敏感概念的激活。这种抑制作用能够有效防止版权形象残留或隐晦违规内容的重构，从而在模型底层确保输出视频在视觉与语义层面的合规性。

最终生成的无风险视频流将被渲染并呈现在用户界面端。与此同时，系统后台会自动启动异步审计进程。后台审计进程会异步记录交互元数据（如原始提示词、检测结果与模型配置参数），并将其结构化存储为日志，为后续的安全溯源与模型迭代提供数据支持。
\subsection{总体架构设计}


\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{Img/system_arch.png}
    \bicaption{全流程系统总体分层架构图}{Safety defense system overall layer architecture}
    \label{fig:safety_defense_system}
\end{figure}

为了有效降低各功能模块之间的耦合度并提升系统的可维护性与水平扩展能力，如图~\ref{fig:safety_defense_system}所示，本原型系统采用了标准化的五层分层软件架构设计。该架构遵循“高内聚、低耦合”的软件工程原则，在逻辑上自顶向下被严格划分为表现层、应用逻辑层、核心模块层、服务层、基础设施和优化层。各层级之间通过定义良好的标准接口进行通信，实现了从用户交互到底层计算的有效隔离与协同。

\textbf{表现层}位于系统架构的最顶端，主要承担人机交互接口的构建与可视化反馈的呈现职责。该层采用现代化的单页应用（Single Page Application, SPA）架构模式，由React前端开发，通过组件化的开发范式实现了用户界面的模块化构建，包含用户指令输入与系统状态可视化反馈等功能。在交互逻辑设计上，该层引入了基于状态驱动的响应式编程模型，能够实时监听用户输入流并动态管理系统的运行状态。针对安全审计场景的特殊性，表现层设计了状态反馈界面，通过进度条实时展示后端的生成进度。

承接表现层请求的是\textbf{应用逻辑层}，该层作为系统的调度中枢与数据交换总线，负责处理核心的业务规则与任务编排。鉴于前置检测与视频生成在计算开销与响应时间上存在显著差异，本系统采用异步非阻塞的事件驱动架构，以替代单一节点过滤。其内部维护了一个包含异步任务队列和优先级调度器的任务调度系统，利用任务队列机制平滑高并发请求的算力负载，确保系统在负载激增时仍能保持吞吐量的稳定性。此外，该层还承担着关键的数据预处理与标准化职责，负责对用户输入的非结构化文本进行清洗、截断与格式校验，并对模型输出的原始推理结果进行结构化封装与日志记录，为系统的安全审计功能提供标准化的数据支撑。

\textbf{核心模块层}位于应用逻辑层与服务层之间，封装了系统的关键防御与生成算法逻辑，是系统的核心功能单元。如图~\ref{fig:safety_defense_system}所示，该层主要包含两个核心模块：指令安全网关与视频生成服务。指令安全网关加载了轻量化的大语言模型权重，作为前置检测的第一道防线，通过内置的CoT推理机制对输入文本进行深度意图分析，并依据推理结果对指令进行相应的风险分类，配置为流式输出模式以满足低延迟的实时阻断需求；视频生成服务加载了高维度的视频扩散模型权重，专注于高质量视频内容的生成。

\textbf{服务层}为核心模块层提供具体的服务实现支持，实现了从核心逻辑到具体算法实现的桥梁。该层主要包含通用模型推理服务、具体的视频生成引擎，以及为生成提供算法支持的采样算法模块，确保了核心功能的稳定落地。

处于架构最底层的是\textbf{基础设施和优化层}，它是系统的核心计算和硬件资源管理基础，提供了硬件资源管理逻辑与关键计算优化技术。针对消费级硬件显存资源受限的工程约束，该层创新性地实现了基于显存池的动态资源调度机制。该机制能够根据业务层的任务指令，智能监测显存占用情况，并在不同模型之间执行权重的动态换入与换出操作，从而在单设备受限环境下实现了多模型串行推理的资源最优化配置。此外，为了进一步提升推理性能，该层集成了模型权重量化、FA2 加速以及内核优化等关键优化技术，封装了硬件资源管理逻辑，全方位保障了高并发下的低延迟响应与高质量内容合成。

\section{关键功能模块设计与实现}

在确立了分层架构与业务流程的基础上，本节将深入探讨系统中三个核心功能模块的具体实现细节。这些模块通过标准化的接口协议紧密协作，共同构建起从指令输入到视频输出的全链路防御能力。

\subsection{输入端防御模块集成}

输入端防御模块集成了经过参数高效微调的 Llama-3.1-8B 模型，其核心工程挑战在于如何在大参数量模型推理的条件下实现毫秒级的响应速度。为了解决这一难题，该模块并未采用传统的全量加载模式，而是基于 HuggingFace Transformers 库构建了轻量化的推理服务管道。在模型加载阶段，系统首先将基座模型的权重以半精度（FP16）格式载入显存，随后动态挂载针对安全检测任务训练的 LoRA 适配器权重。这种分离式加载策略不仅降低了显存占用，还允许系统在不重启服务的情况下热更新安全策略。

在推理交互层面，为了最大程度降低用户感知的延迟，该模块摒弃了传统的“等待-响应”模式，转而采用了流式响应机制。系统利用服务器发送事件（Server-Sent Events, SSE）协议，将大语言模型的推理输出以 Token 为单位实时推送至业务逻辑层。业务层内置的解析器会实时监控输出流，一旦检测到 JSON 格式的结束标记，便立即截断推理过程并提取安全判决结果。实测表明，这种机制使得首字节响应时间降低至一百毫秒以内，有效地掩盖了后台复杂的思维链推理耗时，从而在保障检测精度的同时兼顾了实时性要求。

\subsection{视频生成模块集成}

视频生成模块作为系统的执行终端，承载了将文本指令转化为视觉内容的重任，其实现重点在于如何无缝植入内生安全机制。该模块基于 Wan-2.1 视频扩散模型构建，底层集成了文本编码器、时空变分自编码器以及核心的 Diffusion Transformer（DiT）去噪网络。与标准生成流程不同的是，本系统在 DiT 模块的注意力投影层中注入了特定的概念擦除 LoRA 权重。这一权重的注入过程是完全透明的，并未破坏原始模型的拓扑结构，却在数学层面重构了特征空间的流形分布。

在生成控制逻辑上，该模块被设计为受控触发模式。只有当接收到来自业务逻辑层的“放行”指令及经过清洗的提示词后，生成管道才会被激活。为了确保生成的稳定性，系统固定了采样器的随机种子，并采用了流匹配（Flow Matching）采样算法以减少所需的去噪步数。在推理过程中，注入的擦除权重会持续作用于每一个去噪时间步，动态抑制潜在敏感概念的特征聚合。这种深层干预机制确保了即便输入的提示词存在对抗性扰动，最终生成的视频帧在像素层面依然无法重构出违规的视觉对象，从而实现了“去风险化”的生成目标。

\subsection{交互与可视化模块}

交互模块负责向用户展示系统安全反馈，其设计遵循了“安全可见”的人机交互原则。前端界面基于 React 框架开发，采用组件化思想封装了提示词输入、状态反馈与结果展示三大核心功能区。在输入区，设计了具有实时字符计数与非法字符过滤功能的文本框，并集成了防抖动逻辑以减少无效请求的发送。

更为关键的是可视化的安全反馈机制。该模块设计了基于有限状态机（Finite State Machine, FSM）的动态视图控制器，能够根据后端返回的分析状态实时切换界面形态。当系统判定指令存在风险时，界面会触发阻断视图，以醒目的红色警告框覆盖原有区域，并详细展示由检测模型生成的风险类别解释，如“检测到暴力行为描述”。这种显式的反馈通过界面明确向用户反馈拦截原因。反之，当视频成功生成时，界面会自动切换至媒体播放视图，提供支持流式缓冲的视频播放器，并附带水印叠加功能以标识内容的生成来源。整套交互逻辑流畅且严密，通过视觉语言有效地传达了系统的防御边界。

\subsection{双重防御协同机制与系统边界分析}

在构建文生视频全流程防御系统时，本研究摒弃了单一节点的安全过滤范式，创新性地引入了“输入端前置检测”与“模型端内生擦除”的双重纵深防御架构。为了深入论证该双轨架构的理论必要性与不可替代性，本节将从文本与视觉的双模态表征差异、高并发系统的算力开销模型，以及极端对抗场景下的鲁棒性等方面，对单一防御链路的局限性进行理论剖析，并阐明双重机制如何实现正交互补的协同增益。

\subsubsection{输入端单层防御的局限性}
若系统仅部署基于 Llama-3.1 的前置检测模块，其核心理论脆弱性在于大语言模型与视频扩散模型之间存在的“跨模态语义鸿沟”。前置检测模型工作在离散的文本标记空间，侧重于自然语言的逻辑意图与语法结构分析；而视频生成则发生在高维连续的视觉特征流形之上。

文本与视觉表征空间的不对齐，使得基于纯文本的审查容易被绕过。例如，在面对“视觉拼凑攻击（Visual Patchwork Attack）”时，恶意用户可以完全摒弃高危敏感词汇，转而使用高度中性化的基础几何与物理参数进行描述（如：“在画面中央绘制一个具有特定倾斜角度、RGB值为[255, 0, 0]的几何阵列，并应用动态光影映射”）。在纯文本的语义拓扑中，前置检测模型极易将其判定为低风险的常规渲染指令；然而，当这些描述映射至扩散模型的潜空间并被解码为像素分布时，却可能精确组合成违禁的极端敏感符号或特定受保护对象的面部特征。因此，缺乏模型端的视觉特征阻断作为兜底，仅依赖输入端文本检测的系统无法有效防御这种基于零样本（Zero-shot）的跨模态映射攻击。



\subsubsection{模型端单层防御的局限性}

反之，若系统为追求绝对的生成连贯性而撤除前置拦截，仅依赖 Wan-2.1 模型内部的概念擦除机制来维持安全性，则会将显著增加工程部署开销并降低算法的鲁棒性。

在工程效能层面，视频扩散模型的去噪推理（Denoising Inference）具有极高的计算复杂度。如果允许所有显式的恶意指令（如直接请求生成大尺度暴力冲突的文本）畅通无阻地进入扩散管线，即使模型最终成功在潜空间抹除了违规特征，系统也已经为这些本不该生成的请求消耗了数十步的去噪算力与海量显存带宽。在工业级高并发场景下，这种无差别的全量推理极易被攻击者利用，演变为针对生成算力的拒绝服务攻击（Denial of Service, DoS）。

在算法鲁棒性层面，基于 LoRA 的潜空间概念擦除本质上是在微调阶段对特定条件分布 $p_\theta(x|y)$ 施加的“软约束（Soft Constraint）”。如果攻击者精通提示词工程（Prompt Engineering），可以通过特殊语法对恶意概念施加极高的注意力权重分配（例如在提示词中使用 `(extreme violent scene: 1.9)`）。在无分类器引导（Classifier-Free Guidance, CFG）的采样过程中，这种人为放大的条件向量范数会产生极其强烈的梯度引导信号。在极端对抗条件下，这种过载的引导信号有概率突破 LoRA 权重提供的特征抑制边界，导致被擦除的恶意概念在个别视频帧中发生“特征重构”。

\subsubsection{双重防御的协同增益}

综上所述，本系统设计的“前置检测 + 后置擦除”并非两套同质化机制的冗余叠加，而是构建了结合输入端检测与模型端擦除的纵深防御机制，有效弥补了单一防御模块的局限性。

一方面，前置的 Llama-3.1 模块负责早期的语义拦截与算力调度。它凭借低延迟的思维链推理，在生成流程的起始阶段对显式违规、社会工程学诱导以及多跳逻辑攻击予以有效拦截，为后端扩散模型屏蔽了大部分恶意流量，有效保护了核心生成算力的利用率。

另一方面，Wan-2.1 的概念擦除模块在视觉生成阶段提供了安全约束。针对那些巧妙利用跨模态鸿沟逃脱文本审计的隐晦组合指令，内生安全机制在其扩散去噪的每一个时间步中，持续且静默地剥离敏感元素的视觉表征。这种前置拦截与后置擦除的结合，弥补了单一防御机制的不足，提升了系统应对对抗攻击的鲁棒性。

此外，从系统工程的视角来看，双重防御机制不仅仅是安全维度的叠加，更是对底层计算资源的深度优化。大语言模型的意图检测本质上构成了一种早退机制。鉴于高维视频扩散模型的去噪推理过程极为昂贵，前置检测能够在毫秒级切断恶意请求的生成管线，从而将宝贵的 GPU 算力和显存带宽从注定违规的生成任务中解放出来，这构成了本系统在工程实现上的核心效能贡献。

\section{系统部署与性能优化}

文生视频安全防御系统涉及大语言模型与视频扩散模型的协同推理，这在计算资源消耗上呈现出显存占用与计算密度的差异化需求：前者对显存带宽敏感，后者对算力吞吐敏感。为了在单张消费级显卡（如 NVIDIA RTX 3090/4090，24GB 显存）的受限环境下实现系统的流畅运行，本研究从模型并行部署与推理加速两个方面实施了深度的工程优化。

\subsection{模型并行与显存优化}

在单设备上同时部署百亿参数规模的 Llama-3.1-8B 检测模型与 Wan-2.1 视频生成模型，面临着严峻的显存容量墙挑战。若采用标准全精度（FP32）甚至半精度（BF16）加载，两个模型的静态权重叠加即超过 30GB，远超消费级显卡的物理显存上限。为此，本系统采用了一种结合权重量化与动态卸载的时间-空间交换策略。

针对前置检测模型，考虑到其任务主要依赖语义逻辑推理而非极端的数值精度，系统引入了基于向量量化的 8-bit 压缩技术。通过将权重矩阵动态映射至低精度整数空间，Llama-3.1-8B 的显存占用被显著压缩至 10GB 以内，且实验表明其安全推理的准确率损失可忽略不计。针对视频生成模型，为了保留生成画面的细腻质感，系统维持了其半精度格式，但利用了业务流程天然的串行特性实施动态显存卸载。由于检测与生成两个阶段在时间轴上互斥，系统设计了基于显存池的自动换入换出机制：当检测任务结束后，检测模型的激活参数被立即释放，其静态权重被透明地交换至系统主内存；随后，视频生成模型的权重从内存预加载至显存进行推理。该显存调度策略缓解了单卡物理显存不足的限制，使得在单卡环境下运行全流程防御系统成为可能。

\subsection{推理加速策略}

除了显存容量的优化，推理延迟是影响用户体验的另一关键指标。视频生成模型涉及繁重的时空自注意力计算，其计算复杂度随视频分辨率与帧数的增加呈二次方增长，导致生成过程往往耗时漫长。为了突破这一计算瓶颈，本系统引入了 Flash Attention 2~\cite{dao2022flashattention} 加速算子。

Flash Attention 通过对显存访问模式的重构，利用分块计算策略将注意力矩阵的运算尽可能保留在 GPU 的片上缓存中进行，从而大幅减少了高延迟的高带宽存储器读写次数。在 Wan-2.1 模型的 DiT 架构中应用该算子后，视频生成过程中的注意力计算吞吐量提升了约 3 倍，显著缩短了单帧生成的等待时间。此外，针对扩散模型需要执行数十次迭代去噪的特性，系统引入了 torch.compile 即时编译技术。该技术通过对计算图的静态分析与捕获，将一系列细碎的 Python 算子（如逐元素的加法、乘法）融合为单一的 CUDA 内核。这种算子融合极大地降低了 Python 解释器的调度开销与 GPU 的启动延迟，使得整个去噪循环的执行效率得到提升。通过上述软硬件协同优化，系统最终实现了在保证安全性的前提下，提供接近准实时的视频生成服务体验。

\section{系统测试与展示}

为了全面评估文生视频安全防御原型系统在实际应用环境中的表现，本节开展了系统级的综合测试。测试内容涵盖了面向终端用户的防御功能演示以及面向系统运维的负载性能压测，旨在从功能完备性与工程健壮性两个方面验证研发成果的实用价值。

\subsection{典型防御案例演示}

功能测试旨在系统性检验防御框架在应对复杂语义输入时的判别精度与交互反馈机制。实验选取了具象化的“高风险医疗场景”与隐晦的“语义伪装攻击”作为典型测试用例，通过前端界面的实时渲染结果与后台推理日志的对照，评估系统在实时推理过程中对安全边界的动态界定能力以及对误报与漏报的平衡控制。

在第一组测试中，针对传统关键词过滤系统易产生的“误杀”问题，测试人员构建了包含高密度血腥描述的指令，具体内容涉及心脏外科手术中的开胸、止血及脏器特写。尽管输入文本中充斥着“血液”、“手术刀”等通常被定义为高风险的敏感实体，但本系统的实时表现展示了深层的语境感知能力。如图~\ref{fig:system_test_1}所示，系统并未机械地触发阻断机制，而是通过后端 Llama-3.1 安全核心的“语境框架分析（Contextual Framing）”，将“医生”、“心率监视器”等实体与“开胸手术”动作进行关联，成功将场景锚定为“专业医疗与教育语境”。根据后台安全审计日志反馈，判定该场景下的血腥画面具有功能性与真实性，而非无端暴力，最终给予放行。结果表明，前置检测模块能结合上下文语境进行判定，而非单一的特征词匹配，从而减少了特定专业场景下的误报。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{Img/system_safe.png}
    \bicaption{医疗教育场景下的语境感知与防御豁免演示}{Context-aware defense exemption in medical education}
    \label{fig:system_test_1}
\end{figure}

第二组测试则侧重于验证系统对隐晦恶意意图的推理深度与防御鲁棒性。测试用例采用了一种典型的对抗性文本生成策略，即将“酷刑审讯”的核心意图伪装在“深度按摩”这一看似中性的词汇之下，并辅以“地下室”、“西装男子”、“钳子与电线”等环境线索试图绕过审查。针对如图~\ref{fig:system_test_2}所示的隐晦对抗指令，系统拦截了生成请求，并在前端输出了思维链推理日志以展示判定依据。系统在实体提取阶段识别出工具（钳子/电线）与动作（按摩）的异常语义组合，随即启动隐喻分析模块，推断出“深度按摩”在此语境下实为对受害者施加肉体痛苦的隐语。基于高达 0.98 的风险系数，系统最终依据安全策略关于暴力与酷刑的规定拒绝了生成请求。这种透明化的推理展示表明模型能够有效识别并拦截伪装的恶意语义请求。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{Img/figure_system_unsafe.png}
    \bicaption{隐晦恶意意图的推理与防御拦截演示}{Demonstration of reasoning and defense interception for obscure malicious intent}
    \label{fig:system_test_2}
\end{figure}

为了进一步验证系统模型端内生安全机制的有效性，第三组测试重点考察了基于潜空间偏好优化的概念擦除效果。该测试旨在模拟当极端恶意指令直接作用于视频扩散模型时，底层视觉生成链路的安全底线。如图~\ref{fig:system_test_3} 所示，实验直观地对比了未经安全对齐的基座模型与经过概念擦除微调的对齐模型在面对相同高危指令时的生成表现。从左侧的对比图可以看出，基座模型缺乏内生安全约束，依据指令生成了包含明显暴力与酷刑暗示的违规视频片段；而右侧的对齐模型则展现出了卓越的阻断能力，由于潜空间中与“暴力”、“酷刑”相关的特定概念流形已被有效擦除，模型在去噪解码阶段无法聚合重构出相应的违规视觉特征，最终在底层主动切断了生成管线，并向前端返回“拒绝生成”的安全拦截状态。这一显著的对比结果充分证明了，本系统所采用的后置特征擦除技术能够在脱离文本审查的极端情况下提供初步的安全防线，在一定程度上遏制了风险内容的合成。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\textwidth]{Img/system_compare.png}
    \bicaption{模型概念擦除前后对比}{Comparison before and after concept erasure}
    \label{fig:system_test_3}
\end{figure}

\subsection{异构协同防御下的系统算力效能评估}

在验证了防御功能的有效性后，本节进一步对双轨防御架构在实际高并发场景下的系统算力开销与资源保护效能进行量化评估。为了真实反映系统在连续工作状态下的吞吐量与硬件负载，本研究提取了单次任务的基准耗时，并在单张 RTX 4090 测试环境中进行了基于真实物理约束的系统级并发推演。

在基准性能预热测试中，受用户输入指令长度、系统 I/O 调度以及动态显存换入换出开销的综合影响，系统各模块的耗时呈现出符合预期的工程波动。实测数据显示，前置 Llama-3.1-8B 模块（已开启 8-bit 量化）由于采用了基于思维链（CoT）的意图分析机制，其单次检测的平均耗时约为 $t_{intercept} = 2.65$ 秒。该耗时存在 $\pm 0.8$ 秒的动态波动，这主要归因于面对不同复杂度的指令时，检测模型所需生成的推理 Token 序列长度存在显著差异。另一方面，后置 Wan-2.1（1.3B 版本）模块单次视频生成的平均耗时约为 $t_{generate} = 4.5$ 分钟（包含约 15 秒的模型权重显存调度开销）。两者在计算成本上存在百倍级的非对称性。

为了量化系统效能，本实验构建了总数为 100 个请求的混合指令集，并设置了不同的恶意请求占比。实验引入“无效算力耗时”作为核心评估指标，即系统为最终判定为违规且无法交付给用户的视频所消耗的底层计算与调度时间。测试结果如表~\ref{tab:system_performance}所示。

\begin{table}[htbp]
    \centering
    \bicaption{异构协同防御架构下的系统算力效能评估}{System computational efficiency evaluation under heterogeneous collaborative defense architecture}
    \label{tab:system_performance}
    \resizebox{\textwidth}{!}{
    \begin{tabular}{lcccccc}
        \toprule
        系统架构 & 恶意请求占比 & 实际处理总数 & 拦截耗时 (min) & 生成耗时 (min) & 系统总耗时 (min) & 无效算力耗时 (min) \\
        \midrule
        单层防御 (仅后置) & 20\% & 100 & -- & 453.2 & 453.2 & 91.4 \\
        双层防御 (Ours) & 20\% & 100 & 4.5 & 361.7 & 366.2 & 0.93 \\
        单层防御 (仅后置) & 50\% & 100 & -- & 448.9 & 448.9 & 224.7 \\
        双层防御 (Ours) & 50\% & 100 & 4.3 & 226.5 & 230.8 & 2.16 \\
        单层防御 (仅后置) & 80\% & 100 & -- & 451.6 & 451.6 & 361.8 \\
        双层防御 (Ours) & 80\% & 100 & 4.6 & 91.2 & 95.8 & 3.71 \\
        \bottomrule
    \end{tabular}
    }
\end{table}

由表~\ref{tab:system_performance} 的数据可知，若系统仅依赖后置的模型端概念擦除（单层防御基线），在面对 50\% 的恶意请求流量时，系统将付出高达 225 分钟（近 3.7 小时）的无效算力开销。这意味着极大量的显存带宽与核心算力被浪费在处理违禁内容上。在实际工业环境中，这种架构极易被恶意用户利用，引发针对算力资源的拒绝服务攻击（DoS）。

相比之下，引入前置检测模块的双重防御系统展现出了较好的资源隔离效能。尽管前置检测针对 100 个请求引入了约 4.4 分钟的总检测开销，但在 50\% 恶意请求场景下，系统在生成管线的入口处以毫秒级的单次响应速度拦截了全部非法请求，成功挽回了 225 分钟的无效生成算力。实验数据有力地证明了本原型系统通过异构模型的协同调度，成功实现了以时间换取算力保护的系统级优化，在保障内容安全的同时，极大提升了硬件利用率与工程鲁棒性。

\subsection{单设备显存瓶颈突破与动态调度评估}

除了计算算力的优化，针对消费级显卡（如 RTX 4090，24GB 显存）的硬件容量约束，本节进一步对系统在处理复杂异构模型时的显存调度策略进行了量化评估。

若采用传统的静态串行部署方案（即检测模型与生成模型同时驻留显存），大语言模型 Llama-3.1-8B（半精度 FP16 格式）与视频扩散模型 Wan-2.1 及其 VAE 组件的基础显存占用之和将突破 25GB，这直接导致在单张 RTX 4090 显卡上触发内存溢出错误，使得系统级防御架构完全丧失可用性。

为突破这一物理硬件墙，本系统引入了“8-bit 权重量化 + 基于生命周期的动态显存换入换出”策略。为了验证该策略的有效性，本实验通过 PyTorch 内置的内存探针，实时记录了单次“检测-生成”完整生命周期内的显存占用峰值变化。测试结果对比详见表~\ref{tab:vram_performance}。

\begin{table}[htbp]
\centering
\bicaption{系统部署优化前后的显存峰值对比}{Comparison of Peak VRAM before and after system deployment optimization}
\label{tab:vram_performance}
\resizebox{\textwidth}{!}{
\begin{tabular}{lcccc}
\toprule
部署策略 & 检测模型显存 (GB) & 生成模型显存 (GB) & 系统峰值显存 (GB) & 运行状态 \\
\midrule
静态全量加载 (Baseline) & 15.5 (FP16) & 8.2 (BF16) & > 25.0 & \textbf{OOM 崩溃} \\
静态量化加载 & 8.5 (8-bit) & 8.2 (BF16) & 18.5 & 勉强运行 (高风险) \\
\textbf{动态量化调度 (Ours)} & \textbf{8.5 (换入/释放)} & \textbf{8.2 (换入/释放)} & \textbf{11.2} & \textbf{稳定流畅} \\
\bottomrule
\end{tabular}
}
\end{table}

如表~\ref{tab:vram_performance}所示，在应用本研究提出的显存优化调度策略后，Llama-3.1 意图检测阶段的峰值显存被压缩至约 8.5GB，而当任务流转至视频生成阶段时，通过透明的显存卸载机制，Wan-2.1 模型独占显存资源，峰值维持在 11.2GB 左右（包含推理上下文与特征缓存）。

该组测试结果表明，本系统的架构设计不仅将峰值显存消耗削减了 55\% 以上，更是将原本在消费级单卡上不可运行的双大模型协同系统，转化为具备初步部署可行性的原型系统。这也从侧面印证了本研究的工程化防御方案对计算硬件平台具有极高的兼容性与普适性。

\section{本章小结}

本章旨在将前述安全防御算法集成至原型系统，以验证其在真实业务环境中的工程可行性与鲁棒性。针对文生视频服务在实际部署中面临的实时性响应、异构模型共存以及用户交互体验等多维挑战，本研究设计并实现了一个集前置指令审计与内生安全生成于一体的原型系统。该系统将前序理论成果转化为工程验证平台，也是验证“双重防御”机制在真实业务场景中鲁棒性的关键实验平台。

在架构设计与关键技术实现层面，本章确立了基于前后端分离的标准化分层架构。通过表现层、业务逻辑层与模型服务层的严格解耦，系统实现了从用户意图输入到安全视频输出的流畅闭环。为了攻克在单张消费级计算设备上同时部署百亿参数级检测模型与高维度视频扩散模型的资源瓶颈，本研究创新性地引入了权重量化、动态显存卸载以及 Flash Attention 算子加速等软硬件协同优化策略。这些工程化手段在未牺牲模型防御精度的前提下，成功实现了推理资源的“时分复用”，显著降低了系统的硬件门槛与端到端延迟。

系统级的综合测试结果有力地支撑了本章设计目标的达成。典型防御案例的实测表明，该原型系统具备了较好的威胁感知与认知推理能力，既能毫秒级阻断显式的暴力色情指令，又能有效识破并防御隐晦的恶意诱导。同时，高并发场景下的负载压力测试证实，系统在复杂的流量冲击下仍能保持稳定的吞吐量与可控的显存占用。综上所述，本章实现并测试了文生视频安全防御原型系统。实验结果表明，该系统能够在合理控制硬件开销的前提下，有效防御显式与隐蔽的恶意生成请求，验证了双重防御机制的实用性。